import styled from 'styled-components';

const DialStyles = styled.div`
    padding: 0 16px;
    font-size: 18px;

    p {
        margin-bottom: 6px;
    }
    button {
        border: none;
        font-size: 24px;
        color: inherit;
        background-color: inherit;
    }
`;

export default DialStyles;
